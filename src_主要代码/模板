#include <bits/stdc++.h>
using namespace std;
#define int long long
//template<class... T>
//void debug(T... args) {((std::cerr << args << " "), ...) << "\n";}
#define db std::cerr << "--------------------------------" << __LINE__ << "\n"
#define bug(x) std::cerr << #x << ": " << x << '\n';
#define bug2(x,y) std::cerr << #x << ": "<< x << ' ' << #y << ": " << y << '\n';
#define bug3(x,y,z) std::cerr << #x << ": "<< x << ' ' << #y << ": " << y << ' ' << #z << ": " << z << '\n';
#define rep(i, a, b) for (int (i) = (a); (i) <= (b); (i)++)
#define dwn(i, a, b) for (int (i) = (a); (i) >= (b); (i)--)
#define all(x) x.begin(),x.end()
#define fi first
#define se second
#define pb push_back
#define sf(x) scanf("%lld",&x)
#define pf(x) printf("%lld\n",x)
#define el '\n'
#define endl '\n'
#define MAXN 0x3f3f3f3f
#define LL long long
inline void read(int& a)
{
    int s = 0, w = 1;
    char ch = getchar();
    while (ch < '0' || ch>'9')
    {
        if (ch == '-')
            w = -1;
        ch = getchar();
    }
    while (ch >= '0' && ch <= '9')
    {
        s = s * 10 + ch - '0';
        ch = getchar();
    }
    a = s * w;
}

const int N = 50;
const int MOD = 1e9 + 9;
int ans = 0;
int n;
signed main()
{
        ios::sync_with_stdio(0);cin.tie(0);cout.tie(0);
        return 0;
}